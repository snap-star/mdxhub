---
title: "ProfileBadge"
section: "2. Guides"
order: 39
version: "v1.2"
description: "Author profile card with avatar, bio, and social links. Reads from site.config.json."
---

The `<ProfileBadge>` component renders an author profile card with avatar, bio, and social links. Reads from `site.config.json` and is designed for the `/about` page.

### Usage

```mdx
<ProfileBadge />
```
